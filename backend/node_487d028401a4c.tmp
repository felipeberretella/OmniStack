const express = require('express')

const app = express()

app.post('/users', (request, response)=> {
    return response.json({
        evento:'semana OmniSteck 11.0',
        aluno: 'Felipe'

    })
})

app.listen(3333)